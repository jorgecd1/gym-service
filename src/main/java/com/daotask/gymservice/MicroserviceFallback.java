package com.daotask.gymservice.service;

@Component
public class MicroserviceFallback implements GuestServiceClient {
    // implements methods from constructor
    // sets automatically with lombok.Data
}